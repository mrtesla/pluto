#!/usr/bin/env ruby

TOOLS = %w( supervisor disco dashboard varnish monitor )

if TOOLS.include?(ARGV[0])
  require 'shellwords'
  Kernel.exec(Shellwords.join(["pluto-#{ARGV[0]}"] + ARGV[1..-1]))
end

puts "usage: pluto #{TOOLS.join('|')} ..."
exit 1
