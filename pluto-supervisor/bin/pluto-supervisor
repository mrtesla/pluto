#!/usr/bin/env ruby

begin
  require 'pluto/supervisor'
rescue LoadError
  require 'rubygems'
  $:.unshift(File.expand_path('../../lib', __FILE__))
  require 'pluto/supervisor'
end

Pluto::Supervisor::Runner.new(ARGV).run
